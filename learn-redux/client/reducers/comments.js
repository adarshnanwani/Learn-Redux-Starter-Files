const comments = (state = [], action) => {
    console.log(state, action);
  // switch (action.type) {
  //   case "ds":
  //     return state;
  //   default:
  //     return state;
  // }
   return state;
}
export default comments;
